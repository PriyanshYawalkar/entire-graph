# Entire Graph Environment Variables
PORT=3000
NODE_ENV=development

# Databricks Integration (Optional - Falls back to deterministic local AST heuristics if unset)
DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_SERVING_ENDPOINT=databricks-meta-llama-3-1-70b-instruct
